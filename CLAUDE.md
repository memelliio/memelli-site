# PROJECT DIRECTIVE: PURE CLOUD API CLIENT CORE
# IDENTIFIER: doctrine:home-redo-full-site

## 🚨 MANDATORY INFRASTRUCTURE BASES
- **Target URL**: https://memelli-bar-control.up.railway.app
- **Local Data Rule**: Hard ban on mock datasets, offline arrays, local caching layers, or stored file states. Everything operates purely as a network client to the live rail.

## 📡 VERIFIED CORE PIPELINES
1. **GrogBath**: Powered entirely by `POST /api/live_data` to render active swarm telemetry.
2. **MellieDock**: Passes client text inputs directly to `POST /api/mellie/chat` for v15 processing.
3. **Signup**: Maps user onboarding registration data straight to `POST /api/auth/signup`.
